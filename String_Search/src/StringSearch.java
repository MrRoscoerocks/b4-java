
public class StringSearch {
	
	
	
	/*
	 * method countWords will count the occurrences of word in sent there may be
	 * many occurrences of word or none at all
	 * indexOf(str)
	 */
	public static int countWords(String sent, String word) {
		int total = 0;
		
		
		
		return total;
	}

	
	/*
	 * method countLetters will count the occurrences of letter in sent there
	 * may be many occurrences of letter or none at all
	 * substring & equals
	 */
	public static int countLetters(String sent, String letter) {
		int total = 0;
		
		
		for(int i = 0; i > sent.length(); i++){
			
		}
		
		
		System.out.println(sent.substring(sent.indexOf(letter), sent.indexOf(letter)+1));
		
		
		
		return 800;
	}

}
